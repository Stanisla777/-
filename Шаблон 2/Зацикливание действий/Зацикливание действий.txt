ќчень удобный скрипт, где можно обойтись без массивов и циклов. ¬ этом примере разберем по€вление картинок через равный промежуток времени.

   $(window).scroll(function(){
        if ( $(window).scrollTop() >= $($('#blok16zag')).offset().top-200 )  {
            
            goAnimation();
};
    });


var delayS = 0;

    function goAnimation() {
        $('#rrr').find('img').each(function(index, el) {
        $(this).delay(delayS).queue(function(next) {
            $(this).show();
            next();
        });
        delayS += 110;
    });
};

«десь мы сначала прописали на каких услови€х скролла выполнитс€ функци€ goAnimation();
¬ которой #rrr это контейнер, в котором хран€тс€ ну как пример несколько изображений, которые будут по€вл€тьс€. — помощью метода find() мы находим изображени€ в контейнере, с которыми и будем осуществл€ть действи€. — помощью метода delay() задаем промежуток между циклами, тоесть по€влени€ми картинок.
— помощью метода queue() мы создаЄм цикл в котором пишем действие по€вление картинок.
